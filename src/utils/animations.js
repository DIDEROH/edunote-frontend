/**
 * Le design EDUNOTE est désormais plat et sobre : aucune animation
 * décorative (glow, blur-reveal, orbes, scale au survol...). Ce fichier
 * est conservé comme stub inerte pour ne pas casser les imports existants
 * (`useAnimations(containerRef)` est encore appelé dans plusieurs pages) ;
 * il ne déclenche plus aucun effet.
 */
export const useAnimations = () => {};

export const initAnimations = () => {};

export default {
  useAnimations,
  initAnimations,
};
